# Content Editing Guide

Update festival copy and cards in `content/siteContent.ts`.

- `homeFeatures`: homepage editorial tiles
- `storyItems`: Stories page cards
- `filmItems`: Films page cards
- `conversationItems`: Conversations list
- `festivalAnnouncement`: homepage poster section text

All UI components render directly from this file, so non-developers can update content in one place.
